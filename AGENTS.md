# 금융교육 HTML 수업 프로젝트 작업 지침

## 1. 프로젝트 목적과 대상

이 저장소는 **특수학교 전공과 학생 대상 16차시 금융교육 HTML 수업 프로젝트**이다.

모든 차시는 졸업 후 학생이 일상생활에서 가능한 한 독립적이고 안전하게 돈을 사용·관리하도록 돕는 것을 목표로 한다. 금융용어 암기나 계산 숙달보다 실제 상황에서의 **선택, 판단, 행동**을 우선한다.

## 2. 기준 문서와 적용 순서

새 차시를 설계하거나 기존 차시를 수정하기 전에 다음 문서를 모두 읽는다.

1. `AGENTS.md`: 프로젝트 전체 작업 원칙과 구현 규칙
2. `CURRICULUM.md`: 16차시의 순서, 목표, 차시 간 연결
3. `LESSON01_SPEC.md`: 1차시에서 확립한 수업 설계와 화면·조작 기준
4. `lesson01.html`: 실제 구현된 1차시의 디자인과 상호작용 기준
5. `index.html`: 16차시 전체를 선택하는 메인 홈페이지의 카드·연결 상태 기준

서로 충돌할 경우 **사용자의 현재 명시적 요청 → `AGENTS.md` → `CURRICULUM.md` → 개별 차시 명세 → 기존 구현** 순으로 적용한다.

## 3. 파일 작업 원칙

- `lesson01.html`은 1차시 결과물이다. 사용자가 명시적으로 요청하지 않는 한 삭제하거나 수업 기능을 손상시키지 않는다.
- `index.html`은 16차시 전체를 선택하는 메인 홈페이지이다.
- 2~16차시는 기본적으로 `lesson02.html`부터 `lesson16.html`까지 별도 파일로 만든다. 사용자가 다른 파일 구조를 지정하면 그 지시를 따른다.
- 새 `lessonNN.html` 파일이 생기면 메인 홈페이지가 파일 존재 여부를 확인해 해당 카드를 자동으로 활성화하는 구조를 유지한다.
- 새 차시는 기존 차시의 색상, 배치, 조작 방식, 용어 수준을 이어받는다.
- 별도 활동지에 핵심 활동을 의존하지 않는다. 수업 설명, 선택 활동, 피드백, 형성평가를 HTML 안에서 완결한다.
- 요청받지 않은 차시나 기능을 임의로 함께 수정하지 않는다.
- 새 차시를 만들 때 이전 차시의 핵심을 짧게 연결하고, 마지막에는 다음 차시로 이어지는 질문을 둔다.

## 4. 차시 기본 구성

- 수업 시간: **50분**
- 화면 수: **약 30개**
- 학생 참여: **약 10분 간격**으로 배치
- 동기부여 영상: 수업 초반부에 해당 차시 주제와 직접 관련된 **약 5분 길이의 유튜브 영상**을 배치한다. 영상은 수업 화면 안에서 재생할 수 있게 삽입하고, 재생이 어려운 경우를 대비해 유튜브에서 직접 여는 링크도 함께 제공한다. 자동 재생은 사용하지 않으며, 시청 전·후 발문은 `data-note` 강의자 노트에 넣는다.
- 화면 흐름: 실제 생활 사례 도입 → 핵심 개념 → 학생 조작 → 개념 정리 → 적용 활동 → 형성평가 → 핵심 정리 → 다음 차시 연결
- 형성평가: 마지막 부분에 **3문항**
- 최종 정리: 마지막 화면에 **오늘의 핵심 3가지**
- 설명 화면과 학생 활동 화면을 길게 몰아놓지 않고 번갈아 배치한다.

화면 수는 기계적으로 30개를 맞추기보다 수업 호흡과 접근성을 우선하되, 크게 벗어나야 할 때는 이유를 설명한다.

## 5. 학생 화면 작성 기준

- 한 화면에는 한 가지 핵심 메시지나 행동을 둔다.
- 긴 문단을 피하고 핵심어, 그림, 실제 사례, 표, 선택 버튼을 중심으로 구성한다.
- 문장은 짧고 구체적으로 쓰며 추상적인 금융용어는 생활 언어와 함께 제시한다.
- 학생이 말로 답하기 어려울 때 버튼 누르기, 가리키기, 항목 선택으로 참여할 수 있어야 한다.
- 색상만으로 정답, 상태, 교사 화면과 학생 화면을 구분하지 않는다. 글자·아이콘·테두리 변화도 함께 사용한다.
- 학생에게 보이는 안내·조건·피드백 문구는 원칙적으로 **18px 이상**, 주요 활동 버튼의 항목명은 **20px 이상**을 유지한다.
- 가격과 보조 정보도 수업 판단에 필요하면 **18px 이상**으로 표시한다.
- 작은 화면에서도 버튼이 지나치게 좁아지거나 글자가 잘리지 않도록 반응형 배치를 확인한다.

## 6. 조작과 접근성 기준

모든 차시는 다음 조작을 일관되게 제공한다.

- `N`: 강의자 노트 열기/닫기
- `←`, `→`: 이전/다음 화면
- `F`: 전체화면
- 상단의 **이전·다음 버튼**: 마우스나 터치로 직접 이동
- 첫 화면의 이전 버튼과 마지막 화면의 다음 버튼은 비활성화
- 현재 화면 번호와 전체 진행 정도 표시

버튼은 충분한 클릭 영역, 명확한 레이블, 키보드 포커스 표시를 가져야 한다. 학생 활동 버튼을 클릭했을 때 슬라이드 이동 클릭으로 잘못 처리되지 않도록 이벤트를 분리한다.

## 7. 색상과 화면 역할

1차시의 색상 체계를 기본값으로 유지한다.

- 기본 남색: `#0e2a47`
- 청록색: `#0f766e`
- 주황색: `#f47b34`
- 학생 활동 초록색: `#1f9d68`
- 수업 배경: `#fbf7ef`

화면에는 역할을 구분할 수 있는 정보를 둔다.

- 교사 설명 중심 화면: `data-role="teacher"`, 상단 진행선은 주황색
- 학생 선택·조작 화면: `data-role="student"`, 상단 진행선은 초록색
- 교사 설명은 학생 화면에 길게 노출하지 않고 `data-note` 강의자 노트에 저장

## 8. 교육 내용 판단 원칙

- `원하는 것 = 나쁜 소비`로 가르치지 않는다.
- 필요한 지출과 원하는 지출 사이의 **우선순위**를 판단하게 한다.
- 필요와 원함은 물건 자체가 아니라 사람의 상황과 목적에 따라 달라질 수 있음을 보여준다.
- `비싸다 = 원함`, `싸다 = 필요`로 가르치지 않는다.
- 카드결제도 실제 돈을 쓰는 행동임을 명확하게 표현한다.
- 계산이 어려운 학생도 핵심 금융 판단에 참여할 수 있도록 숫자 계산 외의 선택 방식을 제공한다.
- 한 가지 정답만 강요하기보다 주어진 조건, 가진 돈, 이후의 필요를 근거로 선택을 설명하게 한다.
- 금융사기, 대출, 신용을 다룰 때 공포나 비난보다 안전한 확인 절차와 도움 요청 행동을 가르친다.

## 9. 학생 참여와 피드백 설계

- 단순 O/X나 정답 맞히기만 반복하지 않는다.
- 예산 안에서 고르기, 우선순위 정하기, 위험 신호 찾기, 도움 요청 대상 선택하기처럼 실제 생활 의사결정을 포함한다.
- 오답에는 비난 문구를 쓰지 않는다. 상황 조건을 다시 읽게 하거나 다음 판단 기준을 제시한다.
- 가능한 선택이 여러 개인 활동은 합계, 필수 조건, 안전 규칙 등 충족 기준으로 피드백한다.
- 결과만 보여주지 말고 선택 이유를 말하거나 가리킬 수 있는 문장 틀과 선택지를 제공한다.

## 10. 새 차시 제작 절차

1. `CURRICULUM.md`에서 해당 차시의 목표와 전후 연결을 확인한다.
2. `LESSON01_SPEC.md`와 `lesson01.html`에서 화면 밀도, 색상, 조작 방식을 확인한다.
3. 실제 생활에서 만날 수 있는 구체적인 도입 상황을 정한다.
4. 약 30개 화면의 흐름과 약 10분 간격의 참여 지점을 먼저 설계한다.
5. 교사 설명과 학생 조작을 번갈아 구현한다.
6. 형성평가 3문항, 핵심 3가지, 다음 차시 연결 질문을 포함한다.
7. 키보드·마우스·터치 이동과 모든 활동 버튼의 상태·피드백을 확인한다.
8. 작은 글씨, 긴 문장, 화면 넘침, 색상에만 의존한 구분이 없는지 확인한다.

## 11. 완료 기준

새 차시는 다음 조건을 모두 충족해야 완료된 것으로 본다.

- 해당 차시의 생활 행동 목표가 화면 활동과 형성평가에 일치한다.
- 약 10분 간격으로 학생 참여가 있으며, 최소 한 번 이상 실제 생활 의사결정을 한다.
- 말하기가 어려운 학생도 화면 조작으로 참여할 수 있다.
- 교사 설명은 강의자 노트에 충분히 제공되고 학생 화면은 간결하다.
- 진행선 색상이 화면 역할에 맞게 바뀐다.
- `N`, 방향키, `F`, 상단 이전·다음 버튼이 모두 작동한다.
- 첫 화면과 마지막 화면의 이동 제한이 올바르다.
- 형성평가 3문항, 오늘의 핵심 3가지, 다음 차시 연결이 있다.
- 기존 1차시와 시각적·조작적 일관성이 유지된다.
