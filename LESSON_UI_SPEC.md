# 금융교육 차시 공통 UI 명세

이 문서는 6차시에서 확립한 공통 수업 화면 양식을 1~16차시에 일관되게 적용하기 위한 기준이다. 기존 수업 내용과 활동은 보존하면서 상단바와 조작 체계를 공통화한다.

## 1. 상단바

- 높이와 위치가 모든 화면에서 고정된 짙은 남색 상단바를 사용한다.
- 왼쪽에는 `금융생활교육 · N차시`를 표시한다.
- 오른쪽에는 `목록`, `전체화면`, `← 이전`, `현재 / 전체`, `다음 →`을 한 줄로 모은다.
- `목록`은 `index.html`로 이동한다.
- 첫 화면의 `이전`, 마지막 화면의 `다음`은 비활성화한다.
- 버튼은 검은색 또는 짙은 남색 배경, 흰색 글자, 뚜렷한 포커스 표시를 사용한다.
- 좁은 화면에서는 전체화면 버튼을 숨길 수 있지만 목록과 이전·다음 조작은 유지한다.

## 2. 진행선과 화면 역할

- 진행선은 상단바 바로 아래에 둔다.
- `data-role="teacher"` 화면은 주황색, `data-role="student"` 화면은 초록색으로 표시한다.
- 현재 화면 번호와 전체 화면 수를 상단바에서 항상 확인할 수 있게 한다.

## 3. 강의자 노트

- 학생 화면에는 강의자 노트 버튼과 `N` 키 안내를 표시하지 않는다.
- 각 슬라이드의 교사 설명은 `data-note`에 저장한다.
- `N`을 누르면 현재 화면의 노트가 오른쪽 위 별도 패널로 열린다.
- `N`을 다시 누르거나 패널의 `닫기 ×`를 누르면 닫힌다.
- 화면을 이동하면 열린 패널의 내용도 현재 화면의 노트로 바뀐다.

## 4. 화면 수와 전환 화면

- 차시당 30개 화면을 기본으로 한다.
- 별도 이유가 없으면 28~33개로 만든다.
- 내용의 호흡과 접근성을 위해 범위를 벗어날 수 있으나 이유를 기록한다.
- `시작` 같은 한 단어만 크게 놓는 전환 화면은 사용하지 않는다.
- 활동을 소개하는 화면에도 상황, 조건, 확인할 정보, 다음 행동 중 하나 이상을 넣는다.

## 5. 내용 수준과 접근성

- 6~8차시에서 조정한 수준을 이후 차시의 기준으로 삼는다.
- 한 단계 정답 찾기만 반복하지 않고 두세 가지 조건을 함께 비교하게 한다.
- 실제 금융생활에서 보는 거래내역, 예정 결제, 사용 가능 금액, 결제 시점, 최대 총비용, 계약·반품 조건 등을 활용한다.
- 어려운 계산 자체가 목표가 되지 않도록 표, 핵심어, 아이콘, 단계별 피드백을 함께 제공한다.
- 주요 안내는 18px 이상, 선택 버튼 항목은 20px 이상을 유지한다.

## 6. 구현 기준

- 1~5차시는 기존 수업 구현을 보존하면서 `lesson01-05-topbar.css`와 `lesson01-05-topbar.js`로 공통 상단 UI를 적용한다.
- 6~8차시는 `lesson06-08.css`와 `lesson06-08.js`를 공통 UI 기준으로 사용한다.
- 이후 차시는 공통 자산을 재사용하거나 같은 구조의 공통 파일로 확장한다.
- 상단 버튼과 학생 활동 버튼의 이벤트를 분리한다.
- 키보드 `N`, `F`, `←`, `→`와 마우스·터치 버튼을 모두 확인한다.
- 작은 화면에서 버튼·표·문장이 잘리거나 겹치지 않는지 확인한다.
